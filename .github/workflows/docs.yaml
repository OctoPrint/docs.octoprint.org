name: Build & publish docs

on:
  push:
    branches:
      - master
  repository_dispatch:
    types: [docs]

jobs:
  build:
    name: Build docs
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # you must not use anything but include here or the version.json generation will fail!
        include:
          - version: "1.3.12"
            python: "2.7"
            source: "tags"
          - version: "1.4.0"
            python: "3.7"
            source: "tags"
          - version: "master"
            python: "3.7"
            source: "branches"
          - version: "maintenance"
            python: "3.7"
            source: "branches"
          - version: "devel"
            python: "3.7"
            source: "branches"
    steps:
      - name: ⬇ Checkout docs.octoprint.org
        if: github.event_name == 'push' || matrix.version == github.event.client_payload.ref
        uses: actions/checkout@v2
        with:
          path: docs.octoprint.org

      - name: 👀 Determine latest OctoPrint release
        if: github.event_name == 'push' || matrix.version == github.event.client_payload.ref
        id: latest
        uses: pozetroninc/github-action-get-latest-release@master
        with:
          repository: "OctoPrint/OctoPrint"

      - name: 🐍 Set up Python ${{ matrix.python }}
        if: github.event_name == 'push' || matrix.version == github.event.client_payload.ref
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python }}

      - name: ⬇ Checkout OctoPrint
        if: github.event_name == 'push' || matrix.version == github.event.client_payload.ref
        uses: actions/checkout@v2
        with:
          repository: "OctoPrint/OctoPrint"
          ref: ${{ matrix.version }}
          path: OctoPrint

      - name: 🏗 Install OctoPrint
        if: github.event_name == 'push' || matrix.version == github.event.client_payload.ref
        run: |
          cd ${{ github.workspace }}/OctoPrint
          pip install .[develop,docs]

      - name: 🔄 Generate versions.json
        if: github.event_name == 'push' || matrix.version == github.event.client_payload.ref
        run: |
          python ${{ github.workspace }}/docs.octoprint.org/.github/versionselector/versions_from_matrix.py ${{ github.workspace }}/docs.octoprint.org/.github/workflows/docs.yaml versions.json

      - name: 🏗 Build docs
        if: github.event_name == 'push' || matrix.version == github.event.client_payload.ref
        run: |
          cd ${{ github.workspace }}/docs.octoprint.org/.github/versionselector
          python versionselector.py ${{ github.workspace }}/OctoPrint/docs/ ${{ matrix.version }} ${{ steps.latest.outputs.release }} ${{ github.workspace }}/versions.json

      - name: ⬆ Persist build result
        if: github.event_name == 'push' || matrix.version == github.event.client_payload.ref
        uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.version }}
          path: ${{ github.workspace }}/OctoPrint/docs/_build/html/${{ matrix.version }}

  deploy:
    name: Deploy docs
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: ⬇ Checkout docs.octoprint.org
        uses: actions/checkout@v2

      - name: ⬇ Fetch all artifacts
        uses: actions/download-artifact@v2
        with:
          path: artifacts

      - name: 🔢 Count artifacts
        run: |
          count=`ls -1 artifacts | wc -l`
          echo "::set-env name=ARTIFACT_COUNT::$count"

      - name: 🧹 Cleanup all docs
        if: github.event_name == 'push' && env.ARTIFACT_COUNT != '0'
        run: |
          rm -rf docs || true
          mkdir docs
          touch docs/.nojekyll
          cp -r artifacts/* docs

      - name: 🧹 Cleanup regenerated docs
        if: github.event_name == 'repository_dispatch' && env.ARTIFACT_COUNT != '0'
        run: |
          rm -rf docs/${{ github.event.client_payload.ref }} || true
          cp -r artifacts/${{ github.event.client_payload.ref }} docs

      - name: 🚀 Commit & deploy
        if: env.ARTIFACT_COUNT != '0'
        uses: EndBug/add-and-commit@v4
        with:
          add: "docs"
          message: "Publish new build of docs"
          author_name: 'github-actions[bot]'
          author_email: 'github-actions[bot]@users.noreply.github.com'
